import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SRL Chatbot - Self-Regulated Learning Assistant',
  description: 'AI-powered chatbot to facilitate students\' self-regulated learning based on the StSRL framework',
  keywords: ['education', 'SRL', 'self-regulated learning', 'chatbot', 'student support'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  )
}
